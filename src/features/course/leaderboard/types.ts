export interface LeaderboardEntry {
  rank: number;
  name: string;
  score: number;
  level: string;
  avatar: string;
}
export type studentLevel = "beginner" | "intermediate" | "advanced";
export type messageType = "encouragement" | "motivation" | "push";
